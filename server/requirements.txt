fastapi
uvicorn[standard]
python-multipart
soundfile
deep-translator
torchcodec
demucs
google-generativeai
faster-whisper
scipy
ffmpeg-python
edge-tts
bitsandbytes
pyannote.audio  # 4.x có AudioDecoder bug nhưng pipeline fallback resemblyzer OK + OmniVoice cần transformers 5.x conflict với 3.3.2
pedalboard
pyloudnorm
resemblyzer
silero-vad
scikit-learn
# Face detection cho speaker mapping chính xác (Phase 1 HeyGen-style).
# mediapipe FaceMesh: 468 landmarks/face, lip-activity scoring → ground
# truth speaker ID. Pin 0.10.21 vì build mới đôi khi thiếu legacy
# `solutions` namespace.
mediapipe==0.10.21
opencv-python-headless    # video frame I/O cho face_speaker_svc
# insightface CNN gender — OPTIONAL (KHÔNG trong requirements vì kéo
# numba/llvmlite/scikit-image chain gây resolution-too-deep với pip-25+).
# Bật bằng cách install thủ công 1 lần trên pod:
#   pip install --no-deps insightface
#   pip install onnxruntime onnx easydict
# face_speaker_svc fallback geometry heuristic (~75% gender accuracy) nếu
# insightface không có — vẫn chạy bình thường, chỉ CNN gender không apply.
yt-dlp[default,curl-cffi]
douyin-tiktok-scraper
httpx<0.28  # Pin để tương thích với Douyin_TikTok_Download_API khi tích hợp sidecar
# Auth
pyjwt
bcrypt
sqlalchemy
aiosqlite                # SQLite async (local dev)
asyncpg                  # Postgres async (production via DATABASE_URL)
greenlet                 # SQLAlchemy async engine runtime
sentry-sdk[fastapi]  # error tracking (DSN opt-in qua ENV SENTRY_DSN)
email-validator
python-dotenv
google-auth
boto3                # S3-compatible client cho Cloudflare R2 storage
